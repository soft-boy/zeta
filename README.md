## Quick Start

```bash
npm install -g @soft-boy/zeta
```

Authenticate with an API key:

```bash
export ANTHROPIC_API_KEY=sk-ant-...
zeta
```

Then just talk to zeta. By default, zeta gives the model four tools: `read`, `write`, `edit`, and `bash`. The model uses these to fulfill your requests. 
